package com.md.dzbp.utils;

import android.content.Context;
import android.widget.ImageView;

import com.bumptech.glide.Glide;
import com.bumptech.glide.load.engine.DiskCacheStrategy;
import com.md.dzbp.R;

/**
 * Created by Administrator on 2019/2/12.
 */

public class GlideImageLoader  {
//    @Override                          extends ImageLoader
//    public void displayImage(Context context, Object path, ImageView imageView) {
//
//        //Glide 加载图片简单用法
//        Glide.with(context).load((String) path).error(R.drawable.pic_not_found).diskCacheStrategy(DiskCacheStrategy.ALL).into(imageView);
//
//    }
//
//    //提供createImageView 方法，如果不用可以不重写这个方法，主要是方便自定义ImageView的创建
//    @Override
//    public ImageView createImageView(Context context) {
//        RoundRectImageView simpleDraweeView = new RoundRectImageView(context);
//        return simpleDraweeView;
//    }
}
